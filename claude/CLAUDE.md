# Personal preferences

- Respond in Korean (한국어) by default. Code, commands, file paths, and technical identifiers stay as-is. If I switch to English, mirror that instead.
- Keep answers short and summarized by default. Expand only when explicitly asked.
